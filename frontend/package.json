{
  "name": "leech-rs-dashboard",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -p tsconfig.json && node -e \"require('fs').copyFileSync('src/index.html','dist/index.html')\""
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
